<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <ImageButton
            android:id="@+id/btnDeleteCat"
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:src="@android:drawable/ic_menu_delete"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Eliminar"
            app:tint="#D32F2F"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>


        <TextView
            android:id="@+id/txtNombreCatItem"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            tools:text="Nombre Categoría"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginStart="12dp"
            app:layout_constraintStart_toEndOf="@id/btnDeleteCat"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/btnEditCat"/>

        <TextView
            android:id="@+id/txtDetallesCatItem"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            tools:text="IVA: 15% | Impuesto: 5%"
            android:textSize="13sp"
            android:textColor="#757575"
            android:layout_marginTop="2dp"
            app:layout_constraintStart_toStartOf="@id/txtNombreCatItem"
            app:layout_constraintTop_toBottomOf="@id/txtNombreCatItem"
            app:layout_constraintEnd_toStartOf="@+id/btnEditCat"/>

        <TextView
            android:id="@+id/txtEstadoCatItem"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="ACT"
            android:textSize="11sp"
            android:textStyle="bold"
            android:textColor="#2E7D32"
            app:layout_constraintStart_toStartOf="@id/txtDetallesCatItem"
            app:layout_constraintTop_toBottomOf="@id/txtDetallesCatItem"/>

        <ImageButton
            android:id="@+id/btnEditCat"
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:src="@android:drawable/ic_menu_edit"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Editar"
            app:tint="#1565C0"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>